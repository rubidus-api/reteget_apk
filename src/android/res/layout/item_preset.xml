<?xml version="1.0" encoding="utf-8"?>
<!-- One preset: a top bar with the selection box and the actions, then the name, URL and record
     as one wrapping paragraph across the full width. Tap the paragraph to load the preset into the
     URL bar (same as Use); press and hold it (same as Edit) to change the name and URL. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingLeft="6dp"
    android:paddingRight="6dp"
    android:paddingBottom="8dp"
    android:layout_marginBottom="6dp"
    android:background="#f4f7fb">

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <CheckBox
            android:id="@+id/chk_preset_select"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <!-- Actions: short words in 40dp-high touch targets, no button chrome. Latest shows only
             for a preset with an index page. -->
        <TextView
            android:id="@+id/btn_latest"
            style="@style/PresetAction"
            android:text="@string/btn_latest"
            android:visibility="gone" />

        <TextView
            android:id="@+id/btn_use"
            style="@style/PresetAction"
            android:text="Use" />

        <TextView
            android:id="@+id/btn_edit"
            style="@style/PresetAction"
            android:text="Edit" />

        <TextView
            android:id="@+id/btn_move_up"
            style="@style/PresetAction"
            android:text="Up" />

        <TextView
            android:id="@+id/btn_move_down"
            style="@style/PresetAction"
            android:text="Dn" />

        <TextView
            android:id="@+id/btn_delete"
            style="@style/PresetAction"
            android:text="Del"
            android:textColor="@color/btn_danger_text" />
    </LinearLayout>

    <TextView
        android:id="@+id/txt_preset_body"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        android:textSize="13sp"
        android:textColor="#555555"
        android:lineSpacingExtra="2dp"
        android:background="@android:drawable/list_selector_background"
        android:clickable="true"
        android:focusable="true" />

</LinearLayout>
