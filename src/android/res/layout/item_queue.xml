<?xml version="1.0" encoding="utf-8"?>
<!-- One download queue entry, laid out like a preset: selection box and actions on top, then the
     file name, state and URL as one wrapping paragraph, and the progress bar last while running. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingLeft="4dp"
    android:paddingRight="4dp"
    android:paddingBottom="6dp"
    android:layout_marginTop="4dp"
    android:background="#ffffff">

    <LinearLayout
        android:id="@+id/layout_queue_actions"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <CheckBox
            android:id="@+id/chk_queue_select"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />
    </LinearLayout>

    <TextView
        android:id="@+id/txt_queue_body"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        android:textSize="13sp"
        android:textColor="#555555"
        android:lineSpacingExtra="2dp" />

    <ProgressBar
        android:id="@+id/progress_queue"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        android:paddingTop="4dp"
        android:max="100" />
</LinearLayout>
